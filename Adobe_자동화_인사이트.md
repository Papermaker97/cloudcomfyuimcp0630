# Photoshop·After Effects 자동화 인사이트 정리

> 신세계라이브쇼핑 강의를 준비하면서 실제로 부딪히고 검증한 내용들입니다. ExtendScript로 PS/AE를 자동화할 때 반복적으로 나오는 함정과 그 이유를 정리했습니다.

---

## 1. Photoshop 자동화

### 1-1. 스마트 오브젝트 교체는 ActionDescriptor로 합니다

DOM API에 `replaceContents` 같은 간단한 메서드가 없습니다. 아래 방식이 표준입니다.

```javascript
function 스마트오브젝트교체(파일) {
    var 명령 = stringIDToTypeID("placedLayerReplaceContents");
    var 설명 = new ActionDescriptor();
    설명.putPath(charIDToTypeID("null"), 파일);
    설명.putInteger(stringIDToTypeID("pageNumber"), 1);
    executeAction(명령, 설명, DialogModes.NO);
}
```

교체 전에 `document.activeLayer`를 대상 레이어로 지정해야 합니다.

### 1-2. 스마트 오브젝트는 "기존 배치 크기"를 유지하되, 비율이 다르면 왜곡될 수 있습니다

교체 시 새 콘텐츠는 기존 스마트 오브젝트가 가진 변형(스케일)을 그대로 적용받습니다. 원본 이미지 크기가 원래 것과 크게 다르면(특히 가로세로 비율이 다르면) 예상과 다른 크기로 나올 수 있습니다.

로고처럼 소스마다 원본 크기가 들쭉날쭉한 경우, 교체 전 원래 자리(bounds)를 기억해뒀다가 교체 후 그 자리에 비율 유지하며 다시 맞추는 로직이 필요합니다.

```javascript
function 자리에맞춰리사이즈(레이어, 목표너비, 목표높이, 목표중심X, 목표중심Y) {
    var 바운즈 = 레이어.bounds;
    var 현재너비 = 바운즈[2].as("px") - 바운즈[0].as("px");
    var 현재높이 = 바운즈[3].as("px") - 바운즈[1].as("px");
    var 배율 = Math.min(목표너비 / 현재너비, 목표높이 / 현재높이) * 100; // contain 방식
    레이어.resize(배율, 배율, AnchorPosition.MIDDLECENTER);
    var 새바운즈 = 레이어.bounds;
    var 새중심X = (새바운즈[0].as("px") + 새바운즈[2].as("px")) / 2;
    var 새중심Y = (새바운즈[1].as("px") + 새바운즈[3].as("px")) / 2;
    레이어.translate(목표중심X - 새중심X, 목표중심Y - 새중심Y);
}
```

### 1-3. 텍스트 자동 줄바꿈은 "포인트 텍스트"가 아니라 "박스(단락) 텍스트"여야 합니다

포인트 텍스트(`TextType.POINTTEXT`)는 한 줄로 끝없이 늘어납니다. 길이가 가변인 텍스트(상품명 등)는 박스 텍스트로 강제 전환해야 자동 줄바꿈이 됩니다.

```javascript
if (레이어.textItem.kind !== TextType.PARAGRAPHTEXT) {
    레이어.textItem.kind = TextType.PARAGRAPHTEXT;
    레이어.textItem.width = UnitValue(박스너비, "px");
    레이어.textItem.height = UnitValue(박스높이, "px");
}
```

디자이너가 PSD에서 미리 박스 텍스트로 만들어두는 게 제일 안전하지만, 스크립트에서 방어적으로 전환해도 대부분 잘 동작합니다. (단, 전환 시 기준점이 살짝 달라질 수 있어 실제 파일로 검증 필요)

### 1-4. 배경색 자동 추출은 히스토그램 대신 컬러 샘플러로 충분합니다

전체 이미지 평균색을 뽑으면 상품 자체 색과 배경색이 섞입니다. 상품 사진은 보통 중앙에 상품, 가장자리가 배경이라는 가정하에 모서리 근처 픽셀 하나를 샘플링하는 게 훨씬 간단하고 실무적으로 잘 맞습니다.

```javascript
var 임시문서 = app.open(이미지파일);
var 좌표X = Math.round(임시문서.width.as("px") * 0.03);
var 좌표Y = Math.round(임시문서.height.as("px") * 0.03);
var 샘플러 = 임시문서.colorSamplers.add([UnitValue(좌표X, "px"), UnitValue(좌표Y, "px")]);
var 색 = 샘플러.color.rgb; // {red, green, blue}
임시문서.close(SaveOptions.DONOTSAVECHANGES);
```

정확한 모서리(0,0)는 안티에일리어싱으로 색이 튈 수 있어 3% 정도 안쪽을 씁니다.

### 1-5. 리사이즈 시 "여백 채우기(fit+pad)" vs "꽉 채우기(crop+fill)"는 자동으로 정답이 없습니다

정사각형에 가까운 원본을 세로형(9:16) 등으로 강제 변환하면 여백이 크게 남을 수 있습니다. "원본 훼손 금지"를 우선하면 여백이 생기고, "꽉 찬 화면"을 우선하면 크롭으로 일부가 잘립니다. 둘 다 정당한 선택이라 자동화 설계 초기에 명시적으로 정해야 합니다. 애매하게 두면 나중에 결과물 품질 이슈로 돌아옵니다.

### 1-6. BACKGROUND 레이어는 반드시 일반 픽셀(래스터) 레이어여야 합니다

`selection.fill()`은 래스터 레이어에만 안정적으로 동작합니다. 모양 레이어나 조정 레이어면 실패하거나 의도와 다르게 동작할 수 있습니다. 자동 채우기 대상 레이어의 "종류"를 템플릿 제작 규칙에 명시해야 합니다.

---

## 2. After Effects 자동화

### 2-1. Essential Graphics(EGP)는 "레이어"가 아니라 "속성"을 드래그하는 경우가 대부분입니다

| 대상 | 드래그하는 것 |
|---|---|
| 텍스트 | 레이어의 Source Text 속성 |
| 숫자(가격 등) | Slider Control 이펙트 자체 |
| 색상 | Fill 등 컬러 속성 |
| 미디어(이미지/영상) | **레이어 자체** (유일한 예외) |

미디어 교체 슬롯만 레이어 전체를 드래그한다는 게 헷갈리기 쉬운 포인트입니다.

### 2-2. `effect("이름")`은 표현식이 걸린 "그 레이어 안"에서만 찾습니다

다른 레이어에 있는 이펙트를 참조하려면 `thisComp.layer("레이어이름").effect("이름")`처럼 명시해야 합니다. 이걸 몰라서 "effect가 없다"는 에러를 자주 만납니다.

### 2-3. 이펙트 파라미터를 이름으로 부르면 언어판마다 깨집니다

`effect("가격")("슬라이더")`는 한국어판 전용입니다. 영문판이면 파라미터 이름이 "Slider"라 똑같이 에러가 납니다. **인덱스(숫자)로 부르면 언어 상관없이 항상 동작합니다.**

```javascript
// 위험: 언어 의존
target = effect("가격")("슬라이더");
// 안전: 언어 무관
target = effect("가격")(1);
```

### 2-4. `sourceRectAtTime()`은 "레이어 자기 자신 기준 좌표"를 돌려줍니다

컴포지션 전체 기준 절대좌표가 아닙니다. 화면상 실제 위치를 구하려면 레이어의 `transform.position`을 더해서 변환해야 합니다.

```javascript
var rect = 레이어.sourceRectAtTime(time, false);
var 화면상_left = 레이어.transform.position[0] + rect.left;
var 화면상_top  = 레이어.transform.position[1] + rect.top;
```

### 2-5. Shape Layer 안 Rectangle Path의 Position은 "로컬 좌표", Layer 자체의 Transform Position은 "절대 좌표"

이 둘을 헷갈리면 정확히 이런 사고가 납니다: 계산은 맞는데 값을 엉뚱한 속성에 넣어서 도형이 캔버스 밖으로 튕겨 나가 "사라진 것처럼" 보임.

- 절대좌표(컴포지션 전체 기준) 값을 넣을 곳 → **Shape Layer 자신의 Transform > Position**
- Rectangle Path 자신의 Position은 그 레이어 내부 기준 상대좌표라, 절대좌표를 넣으면 기존 위치 위에 또 더해져서 이중으로 밀림

### 2-6. 여러 레이어를 하나의 배경 박스로 묶을 때는 "필요한 축만" 동적으로

상품명 + 가격을 하나의 하단바가 감싸는 구조에서, 세로 위치는 텍스트 길이가 바뀌어도 변하지 않습니다. 가로 폭만 표현식으로 계산하고, 세로 위치는 그냥 드래그로 한 번 배치하는 게 훨씬 안전합니다. "동적으로 만들 수 있다고 해서 다 동적으로 만들 필요는 없다"는 원칙이 여기서 제일 크게 체감됩니다. 정말 여러 레이어의 절대좌표를 합쳐야 한다면:

```javascript
var nameLayer = thisComp.layer("PRODUCT_NAME");
var priceLayer = thisComp.layer("PRICE");
var nameRect = nameLayer.sourceRectAtTime(time, false);
var priceRect = priceLayer.sourceRectAtTime(time, false);
// 각 레이어의 position을 더해 컴포지션 기준 좌표로 변환한 뒤 min/max로 합집합 박스를 구함
```

### 2-7. 렌더 큐는 "그 순간의 컴포지션 상태"를 참조하지, 스냅샷이 아닙니다

같은 컴포지션으로 여러 버전을 순차 렌더하려고 큐에 N개를 먼저 다 등록해두고 마지막에 한 번에 렌더하면, **전부 마지막으로 세팅한 데이터로 렌더됩니다.** 데이터를 바꿀 때마다 그 자리에서 렌더를 완료(`renderQueue.render()`, 동기 실행)하고 나서 다음 데이터로 넘어가야 합니다.

```javascript
for (var i = 0; i < 상품목록.length; i++) {
    // 이미지/텍스트 교체
    // 렌더큐에 추가 + 출력 경로 지정
    app.project.renderQueue.render(); // 여기서 끝날 때까지 기다림
}
```

### 2-8. 푸티지 교체(`replace()`)는 스마트 오브젝트처럼 자동으로 맞춰주지 않습니다

원본 스케일(%) 값을 그대로 유지한 채 소스만 바뀝니다. 원본 해상도/비율이 다른 이미지로 교체하면 화면상 크기가 달라질 수 있습니다. 이 문제를 피하려면 애초에 입력 이미지들의 비율을 통일해서 넣거나, 별도의 fit 로직을 추가해야 합니다.

### 2-9. "OUTPUT이 MAIN을 감싸는" 구조는 실제로 MAIN을 레이어로 넣어야 완성됩니다

편집용 컴포지션(MAIN)과 최종 렌더용 컴포지션(OUTPUT)을 분리하는 패턴은 흔하지만, OUTPUT 안에 MAIN을 실제 레이어로 드래그해 넣는 걸 깜빡하면 렌더 결과가 통째로 검은 화면이 됩니다. 스크립트가 자동으로 채워주는 부분이 아니라, 템플릿 제작 시 한 번 수동으로 해둬야 하는 구조적 단계입니다.

### 2-10. "파일 쓰기 허용" 설정이 꺼져 있으면 스크립트의 모든 파일 입출력이 막힙니다

편집(After Effects) > 환경 설정 > 스크립팅 및 표현식 > **"스크립트가 파일에 쓰고 네트워크에 액세스하도록 허용"**. 이게 꺼져 있으면 config 파일을 못 읽거나 결과를 저장 못 합니다.

### 2-11. ExtendScript가 멈추면 AE 전체가 같이 멈춥니다

스크립트는 AE의 메인 스레드에서 실행됩니다. 스크립트가 느린 작업(대용량 파일 읽기 등)에 걸려 멈추면 **"실행 취소" 버튼조차 눌리지 않는** 상태가 됩니다. 이때는 작업 관리자로 강제 종료해도 디스크에 저장된 파일은 안전합니다 (저장 안 된 변경사항만 날아감).

### 2-12. 프로젝트 파일은 상위 버전에서 만들면 하위 버전에서 안 열릴 수 있습니다

배포 대상 컴퓨터의 AE 버전을 모른다면, 파일 > 다른 이름으로 저장 > 이전 버전 사본으로 저장해두는 게 안전합니다.

---

## 3. 공통으로 통하는 설계 원칙

### 3-1. 엔진(코드/템플릿)과 데이터(JSON)를 분리하면 자동화가 "안전하게" 반복 가능해집니다

스크립트/템플릿은 검증 후 고정, 상품명·가격·이미지 경로 같은 값만 별도 데이터 파일(JSON)로 관리합니다. 데이터 파일 문법 실수(쉼표 누락, 따옴표 누락)가 비개발자가 만나는 오류의 대부분을 차지하므로, 에러 메시지를 최대한 친절하게(그 언어로, 원인까지) 만들어주는 게 실질적으로 제일 효과가 큽니다.

### 3-2. "자동화가 데이터를 바꿨는데 결과가 그대로"인 착시를 조심해야 합니다

템플릿을 만들 때 넣어둔 예시 값(상품명, 가격 등)과 테스트용 데이터 파일의 값이 우연히 같으면, 자동화가 정상 동작해도 "바뀐 게 없어 보이는" 상황이 생깁니다. 반드시 원래 값과 다른 값으로 한 번 테스트해서 실제로 반영되는지 눈으로 확인해야 합니다.

### 3-3. 한글 인코딩은 BOM 포함 UTF-8로 저장해야 ExtendScript에서 안전합니다

한글 주석/문자열이 든 .jsx 파일은 BOM 없이 저장하면 실행 환경에 따라 깨질 수 있습니다. 반대로 JSON 데이터 파일은 BOM을 붙이면 파서에 따라 파싱이 깨질 수 있어 BOM 없이 저장해야 합니다. 용도별로 다르게 취급해야 합니다.

### 3-4. 라이브 데모는 "테스트 안 된 즉흥 시도"를 절대 하지 않는 게 만족도에 더 좋습니다

준비된 실습을 끝까지 순서대로 진행하는 편이, 현장 분위기에 맞춰 즉흥적으로 새로운 걸 보여주는 것보다 결과가 안정적입니다. 새로운 아이디어는 반드시 사전에 검증한 뒤에만 반영합니다.

### 3-5. AI에게 질문할 때 "그라운딩 문서"를 먼저 주면 헛다리를 크게 줄입니다

레이어 이름, 설정 파일 스키마, 자주 나는 에러와 실제 원인을 미리 정리한 문서를 대화 맨 앞에 붙여주면, 범용 챗봇도 그 프로젝트 기준으로 정확하게 답합니다. "코드를 새로 짜드릴게요" 같은 엉뚱한 제안을 막는 가장 효과적인 방법입니다.
