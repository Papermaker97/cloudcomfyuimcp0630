# 신세계라이브쇼핑 실습 자료 — 제작 현황 및 마무리 안내

> 대상 강의: 2026-07-28(화) 신세계라이브쇼핑_방송 업무 AI 자동화 공통과정
> 이 문서: 실습 파일 패키지(`CLASS_FILES/`) 무엇이 완성됐고, 강사님이 무엇을 채워 넣어야 하는지 정리.

---

## 1. 지금 완성된 것 (코드/문서 — 바로 사용 가능)

| 산출물 | 위치 | 상태 |
|---|---|---|
| 폴더 구조 (교안 명시대로) | `CLASS_FILES/01~05 + 99_BACKUP` | ✅ |
| **규격변환.jsx** (PS 일괄 규격 변환) | `04_SCRIPT/` | ✅ 한글 주석·UTF-8 BOM·한국어 오류 안내 |
| **배너채우기.jsx** (PS 스마트오브젝트·텍스트 교체) | `04_SCRIPT/` | ✅ |
| **영상채우기.jsx** (AE 푸티지·텍스트 교체·렌더큐) | `04_SCRIPT/` | ✅ 파일쓰기 권한 확인 포함 |
| **config.json** (데이터 파일) | `04_SCRIPT/` | ✅ BOM 없음(파싱 안전) |
| **표현식_예시.txt** (박스 리사이즈 · 가격 카운트업) | `04_SCRIPT/` | ✅ |
| **프롬프트_예시.txt** (PART 5 AI 요청 템플릿 6종) | `04_SCRIPT/` | ✅ |
| **업무적용안_워크시트** (PDF + HTML) | `04_SCRIPT/` | ✅ 인쇄/패들렛 배포용 |
| 수강생용 README | `CLASS_FILES/README_수강생용.txt` | ✅ |
| 템플릿 제작 안내(PS/AE) · 백업 안내 · 입력 안내 | 각 폴더 `_..._안내` | ✅ |
| 압축 스크립트(점검 포함) | `package_class_files.sh` | ✅ |

### 설계 원칙 준수
- **"코드는 만지지 않는다. 데이터만 바꾼다"** — 스크립트(엔진)는 고정, `config.json`(데이터)만 수정.
- 모든 `.jsx`는 **줄마다 한국어 주석**(강사 낭독용), **UTF-8 BOM 저장**(한글 깨짐 방지).
- 오류는 전부 **한국어 `alert()`** 안내 (레이어 이름/JSON 문법/파일 경로/권한).
- **한글·공백 경로**에서도 동작하도록 File/Folder 객체로 처리.
- **버전 의존 낮은 기본 API만** 사용(대여 노트북 Adobe 버전 미확인 대비), `toLocaleString` 회피.
- 레이어/컴포지션 이름 표준(`PRODUCT_IMAGE`/`PRODUCT_NAME`/`PRICE`/`LOGO`/`BACKGROUND`, `MAIN`/`OUTPUT`)을 스크립트·안내 문서 전부에서 일치.

---

## 2. 강사님이 채워 넣어야 하는 것 (바이너리·생성물)

> 이 3가지는 Adobe/ComfyUI에서 직접 만들어야 하는 것이라 코드로 생성할 수 없습니다.
> 각 폴더의 안내 문서에 **스크립트가 요구하는 정확한 규격**을 적어두었습니다. 그대로 만들면 스크립트가 바로 동작합니다.

| 항목 | 넣을 위치 | 안내 문서 |
|---|---|---|
| `banner_template.psd` | `02_PHOTOSHOP/` | `_banner_template_제작안내.md` |
| `broadcast_template.aep` | `03_AFTER_EFFECTS/` | `_broadcast_template_제작안내.md` |
| `product_01.png` … (ComfyUI 생성) | `01_INPUT/` | `_상품이미지_안내.txt` |
| (선택) 백업 완성본 4종 | `99_BACKUP/` | `_백업_준비안내.md` |

> ComfyUI 이미지 생성은 이 세션에서는 `comfy-cloud` 연결 인증이 안 되어 있어 대신 만들지 못했습니다.
> (강사님 계정에서 생성 예정이며, 교안상으로도 "실습 이미지는 전부 ComfyUI로 생성"이 5교시 시연 소재입니다.)

---

## 3. 패키징 → 패들렛 업로드 절차

```bash
# 1) 위 2번의 파일들을 각 폴더에 넣은 뒤
bash package_class_files.sh          # 필수 파일 점검 후 CLASS_FILES.zip 생성
#    (스크립트/워크시트만 먼저 묶어 미리 올리려면)
bash package_class_files.sh --force
```
- 생성된 `CLASS_FILES.zip`을 패들렛 보드( https://bit.ly/ssgshop_0728 )에 업로드.
- 수강생 안내: "바탕화면에 압축 해제, 폴더/파일 이름 변경 금지".

---

## 4. 배포 전 테스트 체크리스트 (일부러 깨보기 — 교안 §7)

각 항목을 **일부러 재현**해 오류 메시지가 친절한지 확인:

- [ ] 레이어 이름 불일치 (예: `PRODUCT_NAME` → `상품명`) → 한국어 안내 뜨는지
- [ ] `config.json` 쉼표/따옴표 누락 → JSON 4규칙 안내 뜨는지
- [ ] 한글/공백 폴더 경로에서 실행 → 정상 동작하는지
- [ ] 파일 없음/경로 오타 → 어떤 파일이 없는지 알려주는지
- [ ] AE 파일 쓰기 권한 꺼짐 → 켜는 방법 안내 뜨는지
- [ ] `.jsx` 한글 주석이 Adobe에서 안 깨지는지 (BOM 확인 완료, 실기 재확인)
- [ ] 대여 노트북 Adobe 버전에서 `.aep`가 열리는지 (버전 회신 후)

---

## 5. 이수경 매니저 회신용 메모 (참고)

- 실습 자료 **스크립트·예시·워크시트 완성** → 패들렛 업로드 예정(템플릿 PSD/AEP·ComfyUI 이미지 결합 후).
- 빔프로젝터 **무선 연결 가능 여부** 회신 필요 → 가능하나, 영상 미리보기 프레임 드랍 대비 **사전 렌더 mp4 백업**(`99_BACKUP/`) 준비 권장.
- 대여 노트북 **Photoshop/After Effects 버전** 재확인 요청 (AEP 하위 호환 저장 기준 결정용).
